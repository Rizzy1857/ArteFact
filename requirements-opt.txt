# Optional dependencies for ArteFact
# These are not required for core functionality, but enable extra features.

# Note: The following system tools must be installed separately:
# - ExifTool: Install via 'choco install exiftool' on Windows
# - libewf: Required for E01 image support
#   Windows: Use precompiled binaries
#   Linux: sudo apt-get install libewf-dev
#   macOS: brew install libewf

# Memory analysis
volatility3>=2.4.0  # Memory forensics framework
psutil>=5.9.0

# Network analysis
scapy>=2.4.5        # Network packet manipulation
python-magic>=0.4.0  # File type detection

# Progress and UI enhancements
tqdm>=4.65.0        # Progress bars
colorama>=0.4.4     # Cross-platform colored terminal text

# Additional forensic capabilities
yara-python>=4.2.0  # Pattern matching engine
py7zr>=0.20.0       # 7-zip archive handling
python-registry>=1.3.0  # Windows registry parsing
python-evtx>=0.7.4   # Windows Event Log parsing

# Windows-specific dependencies
python-magic-bin>=0.4.14; sys_platform == 'win32'  # Windows magic support

# File and disk analysis
pytsk3>=20220219
tabulate>=0.9
Pillow>=9.0
pypdf>=4.0
python-docx>=0.8.11
mutagen>=1.46
pefile>=2023.2.7
pyelftools>=0.29

# Optional ML carving
numpy>=1.23
scikit-learn>=1.2
joblib>=1.2

# Optional desktop interface
PySide6>=6.5
