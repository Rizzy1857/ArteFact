# Optional dependencies for ArteFact
# These are not required for core functionality, but enable extra features.

# Deep metadata extraction
exiftool  # https://exiftool.org/ (system tool, not a Python package)

# Advanced PDF and image handling (already in main requirements, but can be moved here if desired)
# Pillow
# PyPDF2

# Memory analysis
volatility3  # pip install volatility3

# Network analysis
scapy  # pip install scapy
tshark  # install via system package manager (e.g., choco install wireshark)

# Progress bars and file type detection
python-magic  # pip install python-magic
tqdm  # pip install tqdm

# E01 (Expert Witness) image support
pyewf  # pip install pyewf
